{
  "builds": [
    {
      "src": "Dockerfile",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/check-date",
      "dest": "/"
    }
  ]
}
{ "rewrites": [{ "source": "/(.*)", "destination": "/" }] }
